# ACM Certificate Module
# Passes through the certificate ARN for use in ALB
# Certificates must be created separately via AWS Console/CLI
# Note: Certificate validation happens at ALB creation time

